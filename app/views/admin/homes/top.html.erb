<div class="container">
<div class="row">
<h4 class="col-md-8 offset-md-1 mt-4 bg-light">
<strong>注文履歴一覧</strong></h4>
</div>
<table class="table">
  <thead>
    <tr>
      <th scope="col">購入日時</th>
      <th scope="col">購入者</th>
      <th scope="col">注文個数</th>
    </tr>
  </thead>
  <tbody>
  <% @order_details.each do |o| %>
    <tr>
     <td><%= link_to o.created_at, admin_order_path %></td>
     <td><%= o.customer.name %></td>
     <td><%= o.amount %></td>
    </tr>
</table>
</div>
<% end %>

<div class="row">
<%= paginate @order_details %>
</div>